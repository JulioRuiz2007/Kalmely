.DS_Store
*.log
node_modules/
.env
.env.local
.vscode/
.idea/
# Worker code — deployed via wrangler, NOT served by Cloudflare Pages.
# Keeping the backend out of the repo means it never gets exposed at kalmely.com/worker/*.
worker/
.wrangler/

# Heavy media — keep out of Pages builds (25 MiB limit)
assets/kalmely-1/
*.mp4
*.AAC

# Fuente del e-book: se guarda en ~/Desktop/kalmely-bundles-polaco/_fuente-ebook/
# Fuera del repo porque Pages publica TODO lo que hay aqui dentro.
scalp/.ebook-src.html
