.DS_Store
*.log
node_modules/
.env
.env.local
.vscode/
.idea/
# Worker code — deployed via wrangler, NOT served by Cloudflare Pages.
# Keeping the backend out of the repo means it never gets exposed at kalmely.com/worker/*.
worker/
.wrangler/

# Heavy media — keep out of Pages builds (25 MiB limit)
assets/kalmely-1/
*.mp4
*.AAC

# Espejo bilingüe solo para revisar el copy en local — NUNCA se despliega
scalp/_mirror-en.html
